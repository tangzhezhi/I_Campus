<?xml version="1.0" encoding="utf-8"?>
<!-- 九宫格每一格的布局 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >
	<RelativeLayout 
	     android:layout_width="fill_parent"
	     android:layout_height="fill_parent">
	    <TextView
        android:id="@+id/student_score_userid_item_text"
        android:visibility="gone"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
         />
	   <ImageView
        android:id="@+id/student_score_item_image"
        android:contentDescription="@string/student"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_gravity="center_horizontal" />
	   <TextView
        android:id="@+id/student_score_item_text"
        android:layout_width="fill_parent"
        android:layout_height="25dp"
        android:gravity="left"  
        android:maxLines="1" 
        android:textSize="14sp"
        android:layout_below="@id/student_score_item_image"
         />
	</RelativeLayout>
</LinearLayout>